using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangeRail : MonoBehaviour
{
    public Player6x6 player;
    public Vector3[] rails;

    
   
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    void GetVector(float x, float y, float z) {

        x = player.transform.position.x;
        y = player.transform.position.y;
        z = player.transform.position.z;

       
        

    }
    void GetRails()
    {
        //Vector3 rail1 = GetVector(-2, 0, );

        //if (player.transform.position == Vector3(-2, 0, 0))
        //{

        //}
        //rails[0] = player.transform.position.(Vector3(-2, 0, 0));
        //rails[1] =
        //rails[2] = 
        //rails[3] = 
        //rails[4] =
        //rails[5] =
        //rails[6] =
        //rails[7] =
        //rails[8] =
        //rails[9] =
        //rails[10] =
        //rails[11] =
        //rails[12] =
    }

  
}
